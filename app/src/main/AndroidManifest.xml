<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.redtecnica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Red Tecnica"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RedTecnica">
        <activity
            android:name=".EditarPerfilActivity"
            android:exported="false" />
        <activity
            android:name=".ChatActivity"
            android:exported="false" />
        <activity
            android:name=".CatalogoActivity"
            android:exported="false" /> <!-- 🔹 Login (Pantalla principal) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 🔹 Registro -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" /> <!-- 🔹 Home -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" /> <!-- 🔹 Perfil -->
        <activity
            android:name=".PerfilActivity"
            android:exported="false" /> <!-- 🔹 Historial -->
        <activity
            android:name=".HistorialActivity"
            android:exported="false" /> <!-- 🔹 Main (si aún la usas) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>